# This file is a placeholder.
# Real envs live alongside their service:
#   - voice-service/.env.example  (Python service: NeonDB + LiveKit + provider keys)
#   - dashboard/.env.example       (Next.js dashboard: NeonDB + voice-service URL/token)
